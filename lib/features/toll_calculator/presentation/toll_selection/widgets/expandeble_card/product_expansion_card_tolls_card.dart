import 'package:flutter/material.dart';

class ProductExpansionCardList extends StatelessWidget {
  const ProductExpansionCardList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
